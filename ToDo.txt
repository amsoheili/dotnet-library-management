Dept reminder background service - Done

Add Cancellation Token to apis - Done

Add In memory Caching system - Done

Add Redis Caching system - Done

Error handling - Done

enhance api general responses (use action results)

Do heavy actions: D
    -Insert 1 million books into a library (inserted about 20k)
    -Loop through all of them and check how many of their titles contain a certain word
    -Use cancellation token to stop the calculation if needed
    
Add constraint for ids and pagination properties on controller actions routes - Done

Break Library's Books into another controller (nesting)

Setup Elk (elastic + kibana for logging - read about ProblemDetails) - Done

Add pagination for books and libraries - Done

Implement execution time Filter for our apis - Done 

Implement Idempotency Middleware - Done

Implement Maintenance Mode Middleware - Done

Implement Correlation id Middleware

Add Cors Policy

Save the idempotency records inside redis instead of mysql

Add distributed lock of redis inside the idempotency middleware

Implement Authentication , use built in .net capabilities - Done

Implement Authorization 
     - role based Done
     - policy based ( add some custom handlers(AuthorizationHandler)) - Done
     - claim based Done
     - resource based - Done 
     - permission based
     
Implement feature: -- CANCELL
    user logs in and borrows some books, and retake them
    user can only retake his own book not others books, so we should add a custom policy based AuthorizationHandler 

Implement feature: Done
    each library has it's own admins - Done,
    each admin can lend or take the books of his own library Done

Implement Super admin and granting admin roles - Done

Implement custom Authentication - Done

Custom Hasher service for user passwords - Done

Search param to be able to filter the books or libraries that contain a certain word inside their names - Done

Feature:
    - each library user has a wallet - Done
    - library admin is able to increase a user's wallet credit - Done
    - transactions like increasing wallet credit are saved into a table - Done

    - each library has a subscription plan - Done
    - each subscription plan has monthly and yearly costs - Done
    - (bonus) a library can have different kinds of subscriptions for example gold, diamond or ... - Done

    - users' subscription can activate by admin - Done
    - each user can buy library subscription 
    - activate auto renewal last bought subscription 
    - the subscription cost will be withdrawn from library users automatically wallet
    - if library user has not paid their monthly/yearly subscriptin cost, he/she can not borrow books
    
    - each user can send/recieve credit through their wallet 
    - a user can send credit to another user 

Feature: 
    - Payment system D
    - a user is able to buy something using a certain payment method
        - for now only wallet is available D
    - create an invoice and then pay that invoice D
    - invoice can be created for different kind of payments D
    - create Order, put that order inside the invoice (i used a generic id) D